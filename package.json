{
  "name": "devdocs-server",
  "version": "1.0.0",
  "description": "devdocs.io local server",
  "main": "server.js",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "scripts": {
    "start": "node server.js",
    "build": "parcel build -t node --bundle-node-modules --no-cache --no-source-maps -d . -o server.dist.js server.js"
  },
  "author": "HGS",
  "license": "ISC"
}
